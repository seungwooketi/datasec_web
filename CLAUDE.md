# datasec_web — 작업 지침

`www.datasec.work` · 인공지능데이터·보안연구센터 홈페이지. **대외 공개물이다.**

```bash
python3 build.py                # docs/ 를 다시 굽는다. 의존성 0
python3 build.py --strict       # ⛔ 공개 차단 사유가 있으면 exit 1
python3 -m http.server 8765 --directory docs
```

⭐ **`README.md` 는 사람이 읽는 사용설명서, 이 파일은 고칠 때 지켜야 할 규약이다.**
겹치면 이 파일이 우선한다.

---

## 1. ⛔ 절대 하지 않는 일

- **`data/*.json` 을 손으로 고치지 않는다.** 과제·구성원 값은 사내 정본 엑셀에서 온다 →
  `tools/pull_from_sot.py`. 손으로 고치면 다음 갱신이 조용히 덮는다
- **`docs/` 를 직접 편집하지 않는다.** 전부 생성물이다. 고칠 곳은 `build.py`·`assets/`·`content/`
- **정본 엑셀을 수정하지 않는다.** 읽기 전용이다
- **`--strict` 가 막는 것을 우회하지 않는다.** 게이트는 대외 공개물을 지키라고 있다
- **없는 사실을 쓰지 않는다.** 과제 요약·소식·성과는 **사람이 쓰는 글**이다.
  지어낸 한 문장이 곧 허위 사실이다
- **메일 주소를 HTML 에 평문으로 넣지 않는다.** 반드시 `mail()` 을 거친다
- **엑셀 파일 자체를 사이트에 올리지 않는다** — 시트 탭 이름만으로 실명이 나간다

## 2. 값은 어디서 오는가

```
01. 센터 현황/*.xlsx        사내 정본. 사람이 회사 시스템에서 내려받아 갱신
   └─ tools/pull_from_sot.py    사내에서만 돈다 (openpyxl + keti-kb 어댑터)
        └─ data/*.json          커밋된다. ⭐ 사이트가 읽는 것은 이것뿐이다
             └─ PR              리뷰가 곧 「무엇을 공개할지」의 결정이다
                  └─ docs/      build.py 산출물. Pages 가 그대로 서빙
```

⭐ **런타임에 질의가 없다.** DB 가 죽어도 사이트는 살아 있고, 정본이 틀려도 그 실수는 PR 을
통과해야 공개된다. 자동 게시로 만들면 사람이 고친 값을 다음 회차가 되돌린다.

⚠️ `pull_from_sot.py` 는 **덮어쓰지 않고 합친다.** 정본이 소유한 것(부처·기간·참여형태·담당자)만
갱신하고 사람이 정한 것(slug·영문 제목·영문 이름·링크·사진)은 건드리지 않는다.
새 과제는 **자동으로 싣지 않고 보고만 한다** — slug 와 영문 제목이 없기 때문이다.

## 3. 고치는 자리

| 무엇을 | 어디를 |
|---|---|
| 소개문 · 협력 안내 · 연락처 · 주소 · 표제 | `content/site.json` |
| 소식 한 편 | `content/news/<slug>.ko.md` **+ `.en.md`** |
| 과제 소개 | `content/projects/<slug>.ko.md` **+ `.en.md`** |
| 과제 · 구성원 값 | ⛔ 손대지 않는다 → `tools/pull_from_sot.py` |
| 쪽 구조 · 새 쪽 | `build.py` |
| 색 · 글꼴 · 간격 | `assets/industry.css` — ⚠️ **디자인 시스템이다. 값을 새로 정하지 않는다** |
| 이 사이트만의 배치 | `assets/site.css` |
| 브랜드 이미지 | `assets/brand/` |

## 4. ⛔ 게이트 — 문서가 아니라 코드가 막는다

`build.py --strict` 와 `.github/workflows/check.yml` 이 함께 막는다:

| 막는 것 | 왜 |
|---|---|
| `name_en_confirmed: false` | 실존 인물의 로마자 표기는 **본인이 쓰는 철자**가 정답이다 |
| `sample: true` 인 소식 | 디자인 확인용으로 지어낸 글이다 |
| 없는 과제를 가리키는 구성원 | 404 로 나간다 |
| 미공개 과제가 산출물에 새어 나옴 | 거르기를 믿지 않고 **HTML 을 다시 훑는다** |
| 평문 `mailto:` 나 메일 주소 | 한 군데만 빠져도 그 쪽이 수집 대상이 된다 |
| `docs/` 가 원본과 불일치 | 값만 고치고 다시 굽지 않으면 **옛 내용이 서빙된다** |
| 깨진 내부 링크 | |

⭐ **새 규약이 생기면 문서가 아니라 이 게이트에 넣는다.** 지켜지지 않는 규약을 문서에만
적어 두는 것이 제일 나쁘다.

## 5. 두 언어

- 실제 경로 분리다 — `/ko/…` `/en/…` + `hreflang` + `<html lang>`
- **소식·과제 소개는 두 언어가 다 있어야** 그 언어에 나온다. 한쪽만 있으면 경고만 나고 안 보인다
- ⭐ **과제 소개가 두 언어 다 있어야 상세 쪽이 생긴다.** 없으면 상세 쪽을 만들지 않고
  목록에서 링크도 걸지 않는다 — 빈 쪽으로 보내지 않기 위해서다
- 화면에서 뺀 설명 문단은 `*_desc` 로 남겨 `<meta name="description">` 에만 쓴다
- ⚠️ **그림 안에 글자를 넣지 않는다.** 언어 전환이 안 되고, 선택·검색이 안 되며,
  크기를 줄이면 제일 먼저 죽는다

## 6. 디자인 시스템 (Industry)

핸드오프(`design_handoff_aidsrc_site`)의 시스템을 따른다.

- 강조색은 **강철빛 파랑 `#5980a6` 하나**. 바탕 `#f2f2f3`, 잉크 `#1d1f20`
- **모서리를 굴리지 않고 카드에 면을 채우지 않는다.** 액자는 `.blueprint` + 네 귀의 `+` 등록마크이고
  **등록마크를 빼면 시스템 밖이다**
- 표제 Barlow Condensed / 본문 Barlow / 한글 IBM Plex Sans KR
- ⭐ **한글 표제는 크기를 따로 준다.** 88px 는 라틴 대문자 기준이라 한글에서 과하다.
  ⚠️ `body[data-lang="ko"]` 가 붙어 우선순위가 높으므로 **미디어쿼리 안에서도 같은 선택자로
  다시 써야** 좁은 화면에서 줄어든다
- `word-break: keep-all` 은 **빼면 안 된다** — 없으면 단어 중간에서 줄이 바뀐다
- `.duotone` 은 **사진**에만 쓴다. 브랜드 도해는 자체 색이 곧 내용이라 씌우지 않는다

## 7. ⚠️ 이 저장소가 실제로 밟은 것들

문서로 남기는 이유는 **전부 에러 없이 지나갔기 때문**이다.

| 무슨 일이 있었나 | 그래서 지금 |
|---|---|
| Pages `build_type` 이 `workflow` 인데 배포 워크플로가 없어 커밋 넷이 배포되지 않았다. CI 는 초록, API 는 `built` | `check.yml` 이 `actions/deploy-pages` 로 직접 배포한다 |
| `/assets/site.css` 주소가 안 바뀌어 배포해도 옛 CSS 가 보였다 | 자산 주소에 **내용 해시**를 붙인다(`stamp()`) |
| 홈의 「이메일」 줄만 `mail()` 을 안 거쳐 평문으로 나갔다 | 빌드가 산출물을 훑어 막는다 |
| 정본 과제명의 `(R)/(C)/(N)` 접두·이중 공백 때문에 이름 조인이 18건 전부 실패 | `pull_from_sot.py` 가 정규화한다. 전부 실패하면 경고하고 멈춘다 |
| 로고 부제가 로고 높이의 11.6% 라 48px 에서 5.6px | 부제를 두 줄로 나눠 재조립(24.5%). `tools/` 에 스크립트가 없으므로 로고를 새로 받으면 다시 조립해야 한다 |
| 히어로 그림을 360px 칸에 넣어 라벨이 8.4px | 칸을 560px 로. 그림 라벨은 **표시 크기에서 읽히는지 계산하고 넣는다** |
| 생성 이미지의 알파가 전부 255 — 체커보드가 **픽셀로 그려져** 있었다 | 밝기+채도로 키아웃한다 |
| 생성 이미지 우하단에 워터마크(✦) | 자르거나, 옆 빈 배경을 복사해 덮는다 |

## 8. 정본을 읽을 때 밟는 지뢰

`README.md` §8 에 표로 있다. **다른 스크립트를 새로 쓸 때 반드시 먼저 읽는다** —
`예시` 행 · 접두와 이중 공백 · `과제기간` vs `계정기간` · 「수행 중」이 아닌 `계정기간_2026` ·
머리글 없는 사번 열 · `비고` 열 · 파일마다 다른 인원 수.

## 9. 공개

- GitHub Pages · `main` 브랜치 · `/docs` · Actions 배포
- Cloudflare DNS: `www` 와 `@` 를 `seungwooketi.github.io` CNAME 으로, **둘 다 회색(DNS only)**
- ⛔ **주황 구름(프록시)을 켜지 않는다** — GitHub 의 인증서 발급·갱신이 막힌다
- ⛔ 리포를 지우거나 Pages 를 끄기 전에 **DNS 레코드를 먼저 지운다**

## 10. 커밋

- **왜 그렇게 했는지**를 쓴다. 무엇을 바꿨는지는 diff 가 안다
- 특히 **조용히 깨졌던 것**은 반드시 남긴다 — 에러가 안 나는 결함은 두 번 밟는다
- 값을 고쳤으면 `python3 build.py` 결과를 **같은 커밋에** 넣는다(CI 가 검사한다)
